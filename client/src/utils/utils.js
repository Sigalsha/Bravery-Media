class Utils {
  /*   handleSearchType = (searchType, searchResultsItems) => {
    switch (searchType) {
      case "/movies":
        return (state = "selectSubject");
      case "/books":
        searchResultsItems.map(element => {
            element.volumeInfo.title
        });
      case "PLAY_GAME":
        return (state = "intro");
      default:
        return (state = "intro");
    }
  }; */
}

let utils = new Utils();

export default utils;
